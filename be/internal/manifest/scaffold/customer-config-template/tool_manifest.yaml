tools:
  - name: lookup_sku
    type: python_script
    description: Look up product details by SKU from the catalog
    script: tools/lookup_sku.py
    input_schema:
      type: object
      properties:
        sku:
          type: string
          description: The SKU identifier to look up
      required:
        - sku
      additionalProperties: false
